# Leave blank to use built-in mock data (recommended for first run).
# Once your Google Apps Script web app returns the documented JSON shape,
# paste its /exec URL here and the dashboard will fetch live data.
VITE_APPS_SCRIPT_URL=
